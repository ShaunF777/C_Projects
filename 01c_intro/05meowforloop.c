#include <stdio.h>
#include <cs50.h>

int main(void)
{
    for (int i = 0; i <3; i++)  //Parenthesis now has 3 tasks:
    //1.init a variable(one time); 2.check boolean expression condition and run loop content; 3. do update at end of loop
    {
        printf("meow\n");
    } // Curly braces can be left out if only one line of code in for loop
}
